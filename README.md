# Маскировка и форматирование

Набор функций для обработки персональных данных: маскировки номеров карт и счетов, а также преобразования форматов даты.

## Функционал

Проект включает две основные функции:

1. **`mask_account_card()`** — маскирует номера банковских карт или счетов в строке.
2. **`get_date()`** — преобразует дату из формата ISO в формат «ДД.ММ.ГГГГ».

## Использование

### Маскировка номера карты или счёта

Функция `mask_account_card(card_number)` принимает строку с номером карты/счёта и возвращает замаскированную версию.

**Параметры:**
* `card_number` (`str`) — строка, содержащая тип карты/счёта и номер (например, `"Visa Platinum 7000792289606361"`).

**Возвращает:**
* Строку с замаскированным номером в формате `"<тип> <замаскированный номер>"`.
* `None`, если входные данные некорректны или произошла ошибка маскировки.

**Примеры:**
```python
print(mask_account_card("Visa Platinum 7000792289606361"))
# Вывод: Visa Platinum 7000****6361

print(mask_account_card("Счёт 40817810099910004312"))
# Вывод: Счёт 4081****4312

print(mask_account_card("Invalid input"))
# Вывод: None
```

## Тестирование

Модуль сопровождается набором тестов для проверки корректности работы функций.

### Требования для запуска тестов

* Python 3.8+;
* pytest;
* pytest-cov (для измерения покрытия кода);
* pytest-html (для генерации HTML‑отчётов).

# Processing Module

Модуль `processing` предоставляет функции для фильтрации и сортировки списков словарей по заданным критериям. Предназначен для работы с данными, содержащими поля `state` и `date`.

## Установка

Модуль не требует отдельной установки — достаточно разместить файл `processing.py` в проекте.

## Использование

Импортируйте функции из модуля:

```python
from processing import filter_by_state, sort_by_date
```
## Тестирование

Модуль сопровождается набором тестов для проверки корректности работы функций.

### Требования для запуска тестов

* Python 3.8+;
* pytest;
* pytest-cov (для измерения покрытия кода);
* pytest-html (для генерации HTML‑отчётов).

# Проект: Generators — набор генераторов для работы с финансовыми данными

Этот проект содержит набор юнит‑тестов для проверки корректности работы функций, обрабатывающих данные о банковских транзакциях и генерирующих номера карт.

## Установка

Проект не требует отдельной установки — достаточно скопировать файлы в ваш проект или импортировать модули напрямую.

**Зависимости:** Python 3.6+ (стандартная библиотека, дополнительных зависимостей нет).

## Использование

## Часть 1. Функции модуля `generators.py`

### 1. `filter_by_currency(transactions, currency_code)`

**Назначение:** фильтрация списка транзакций по коду валюты.

**Параметры:**
- `transactions` (`list`) — список словарей с данными транзакций;
- `currency_code` (`str`) — код валюты для фильтрации (например, `"USD"`, `"EUR"`).

**Возвращает:** генератор (`generator`) отфильтрованных транзакций.

**Логика работы:**
- проверяет поле `operationAmount.currency.code` в каждой транзакции;
- возвращает только транзакции с точным совпадением кода валюты.

**Пример использования:**
```python
usd_transactions = list(filter_by_currency(transactions, "USD"))
```
### 2. transaction_descriptions(transactions)
**Назначение:** извлечение описаний операций из списка транзакций.

**Параметры:**

- `transactions` (`list`) — список словарей с данными транзакций.

**Возвращает:** генератор (`generator`) строк‑описаний транзакций.

**Логика работы:**

- извлекает значение поля `description` из каждой транзакции;

- пропускает транзакции без поля description.

### 3. card_number_generator(start, end)
**Назначение:** извлечение описаний операций из списка транзакций.

**Параметры:**

- `start` (`int`) — список словарей с данными транзакци.
- `end` (`int`) — конечное число диапазона.

**Возвращает:** генератор (`generator`)  строк с номерами карт в формате XXXX XXXX XXXX XXXX.

**Логика работы:**

- автоматически корректирует границы:
  - если `start < 1`, берёт `1`;
  - если `end > 9999999999999999`, берёт максимальное значение;
- дополняет числа нулями до 16 знаков;
- разделяет номер на группы по 4 цифры.

## Тестирование

#### `TestFilterByCurrency`
Проверяет фильтрацию транзакций по валютам:
* фильтрация по существующим валютам (`USD`, `EUR`);
* отсутствие совпадений (`RUB`);
* обработка пустого списка транзакций;
* обработка транзакций без поля `operationAmount`.

#### `TestTransactionDescriptions`
Проверяет извлечение описаний транзакций:
* нормальный случай (транзакции с описаниями);
* пустой список транзакций;
* транзакции без описаний;
* смешанный случай (есть и нет описаний);
* одна транзакция с описанием.

#### `TestCardNumberGenerator`
Проверяет генератор номеров карт:
* небольшой диапазон номеров;
* один номер;
* минимальное и максимальное значения;
* граничные случаи (`start < 1`, `end > max`);
* большой диапазон (проверка первых элементов).

## Формат данных

### Транзакция
Пример структуры транзакции:
```json
{
  "id": 939719570,
  "state": "EXECUTED",
  "date": "2018-06-30T02:08:58.425572",
  "operationAmount": {
    "amount": "9824.07",
    "currency": {
      "name": "USD",
      "code": "USD"
    }
  },
  "description": "Перевод организации",
  "from": "Счет 75106830613657916952",
  "to": "Счет 11776614605963066702"
}
```
# Проект decorators содержит набор декораторов для логирования выполнения функций.

Основная цель — предоставить простое и удобное средство для логирования как в консоль, так и в файл.

## Основные возможности:

* Логирование начала и конца выполнения функции.

* Логирование результата выполнения функции.

* Опциональная запись логов в файл.

* Поддержка функций с аргументами и ключевыми аргументами.

* Обработка исключений с логированием ошибок.

### Установка

Для установки декораторов и запуска тестов выполните следующие команды:
 ```
pip install -e .  # Установка пакета
pip install pytest  # Установка pytest для запуска тестов
```

### Тесты

Все тесты находятся в директории tests и покрыты модулем pytest. Для запуска тестов выполните:
```
pytest -v  # Запуск тестов с подробным выводом
pytest -v -s  # Запуск тестов с отображением логов в консоли
```

# Проект utils работает с данными транзакций (загрузка из JSON‑файлов)

## Функции:

`load_transactions(filepath: str) → list` — загружает транзакции из JSON‑файла.

## Использование:

* если файл не существует — возвращает пустой список;

* если содержимое не является списком — возвращает пустой список;

* в остальных случаях — возвращает список транзакций.

# Проект external_api делает конвертацию сумм в разные валюты с использованием внешних API.

## Использование:

* для RUB — возвращает сумму без изменений;

* для других валют — умножает сумму на курс из get_exchange_rate();

* если курс не найден — выбрасывает ValueError

## Установка

* Python 3.8+

* pip (менеджер пакетов Python)

### Тесты

Все тесты находятся в директории tests и покрыты модулем pytest. Для запуска тестов выполните:
```
pytest -v  # Запуск тестов с подробным выводом
pytest -v -s  # Запуск тестов с отображением логов в консоли
```

